// Investment page removed — kept as a stub to avoid build errors while pages are reorganized.
import React from 'react';

const Investment: React.FC = () => {
  return null;
};

export default Investment;